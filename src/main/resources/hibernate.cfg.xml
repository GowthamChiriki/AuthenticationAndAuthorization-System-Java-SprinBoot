<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- JDBC connection settings -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/authsystem?useSSL=false&amp;serverTimezone=UTC</property>
        <property name="connection.username">root</property>
        <property name="connection.password">Gowtham@2929</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Display SQL -->
        <property name="show_sql">true</property>
        <property name="format_sql">true</property>

        <!-- Use HBM2DDL only for development if desired; set to 'validate' or remove in production -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Connection pool (built-in) -->
        <property name="connection.pool_size">5</property>

        <!-- Entities -->
        <mapping class="com.authsystem.entity.Role"/>
        <mapping class="com.authsystem.entity.User"/>
    </session-factory>
</hibernate-configuration>
